import { Modal } from 'antd'
import styled from 'styled-components'

export const MainModal = styled(Modal)`

  .ant-modal-body {
    min-height: 10vh;
  }

  .ant-modal-footer {
    display: none;
  }
`